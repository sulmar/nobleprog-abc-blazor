@if (IsLoading)
{
    <Placeholder Message="Loading..." />
}
else
{
    @ChildContent
}

@code {

    [Parameter]
    public bool IsLoading { get; set; }

    [Parameter]
    public RenderFragment ChildContent { get; set; }
}
